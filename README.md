flowchart LR


### Flow

when the user use a short url
the api is frist gona check if it have cached the endoint if not then the db

they is a comple of ways you can get your url cached 1 is used a lot
2 is a payed version
3 they have not ben loaded tomany urls in the cache


## Plan / options

the client url can be 100 % custome they are gona suplye it

with a payed service you can have multiple shorturl hit the same endpoint so the client can se from are dashboard where they clicks are comming from

one time urls and prem urls

make a dashboard


## dev 

if i wan't som qrue options on the url we don't need to add it to the url but we can add it as agrs in the database eithe way we need to find the endpoint url so that is a query either way if it
